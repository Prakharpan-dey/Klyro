/**
 * Outbound links shown in the interface.
 *
 * These are plain anchors, not requests: nothing is fetched until someone
 * clicks, so the "no off-origin requests" readout in the status bar stays
 * honest. Set a value to an empty string to hide that link entirely.
 */

export const SUPPORT_URL = 'https://buymeacoffee.com/zeusdotdev'
